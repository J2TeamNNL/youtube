# Lập trình web cơ bản buổi 25 PHP Admin

- **id:** `TdEOdeIOVEQ`
- **url:** https://www.youtube.com/watch?v=TdEOdeIOVEQ
- **published:** 2021-12-23
- **duration:** 7204s (~2h0m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-25-php-admin-TdEOdeIOVEQ/`](../../transcripts/lap-trinh-web-co-ban-buoi-25-php-admin-TdEOdeIOVEQ/)
- **topics:** web, php, admin
- **series:** Lập trình web cơ bản — buổi 25
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 25 series Lập trình web cơ bản — PHP Admin (dashboard / CRUD phía quản trị). Đầu buổi chào, chia sẻ nhóm tin / phim Châu Tinh Trì, Spotify và giải trí có chọn lọc; xem video tình cảm rồi Q&A quan điểm tán gái, tình bạn khác giới. Phần kỹ thuật: MySQL `ENUM` (ví dụ giới tính nam/nữ) so với MSSQL — thêm giá trị ENUM trên MySQL dễ buộc ALTER cả cột, copy hàng triệu bản ghi; gợi ý tạo cột mới rồi update từng lô. Nhắc Git conflict (kéo master về nhánh trước khi push); khi bí code nên nghỉ; học bằng thực hành. Ôn giỏ/đặt hàng: có thể dùng JS tính tổng tiền phía client cho UX (PHP vẫn kết nối DB). Sang admin: bắt buộc trang đăng nhập; bảng admin (email, mật khẩu, level — Super admin vs nhân viên); insert tài khoản mẫu; form login → query → lưu `id` + `level` vào session; `require` vs `include`. File `check_admin` chặn chưa đăng nhập; `check_super_admin` (empty/level) hạn chế trang nhạy cảm. Quản lý đơn: folder order, list JOIN customer, format status (mới đặt / đã duyệt / hủy), xem chi tiết JOIN `order_detail` + sản phẩm, nút duyệt/hủy `UPDATE status`. Cuối buổi: preview viết tài liệu đồ án, Noel, chào kết.

## Mục lục

```
0:00 Chào; nhóm tin / phim Châu Tinh Trì (gộp)
1:39 Spotify; giải trí chọn lọc (gộp)
13:31 Thực hành khi bí; thư giãn (gộp)
17:40 Xem video tình yêu; quan điểm bạn gái (gộp)
35:53 Q&A sắc đẹp / tán gái / tình bạn khác giới (gộp)
45:44 MySQL ENUM giới tính; so sánh MSSQL (gộp)
51:47 ALTER ENUM bảng lớn; copy cột an toàn (gộp)
58:11 Git conflict; pull master trước khi push (gộp)
1:00:00 Khi bí code nên nghỉ; học bằng thực hành (gộp)
1:06:01 JS tính tổng giỏ / UX phía client (gộp)
1:11:25 Sang admin; trang đăng nhập bắt buộc (gộp)
1:15:15 Tạo bảng admin; insert Super admin (gộp)
1:17:21 Login form; query email/password; session (gộp)
1:19:05 require vs include; lưu id + level session (gộp)
1:22:24 Chặn chưa login; file check_admin (gộp)
1:29:05 check_super_admin; empty() vs isset (gộp)
1:36:21 Quản lý đơn hàng; folder order (gộp)
1:38:04 List orders JOIN customer; format status (gộp)
1:44:24 Xem chi tiết đơn; JOIN order_detail + product (gộp)
1:49:27 Duyệt / hủy đơn; UPDATE status (gộp)
1:52:02 Preview tài liệu đồ án; Noel; chào kết (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
