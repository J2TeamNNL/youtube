# Lập trình web cơ bản buổi 8 JavaScript làm quen

- **id:** `h4wTJgQxnJg`
- **url:** https://www.youtube.com/watch?v=h4wTJgQxnJg
- **published:** 2021-11-03
- **duration:** 6090s (~1h42m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-8-javascript-lam-quen-h4wTJgQxnJg/`](../../transcripts/lap-trinh-web-co-ban-buoi-8-javascript-lam-quen-h4wTJgQxnJg/)
- **topics:** web, javascript
- **series:** Lập trình web cơ bản — buổi 8
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 8 series Lập trình web cơ bản — JavaScript làm quen: chào, soạn bài, lan man Q&A (học bù không đổi lịch; ra trường thì tự làm 1–2 sản phẩm portfolio; khóa cơ bản chưa đủ đi làm; phỏng vấn phải nghiêm túc đừng “tích kinh nghiệm” rồi bom offer; coder vs developer — copy code vs hiểu/phát triển; tester càng biết CNTT/SQL càng tốt; nhúng SoundCloud/YouTube autoplay, trình duyệt cố tình chặn vì UX) rồi mới vào bài. HTML là xương, CSS là mỹ phẩm, JS là “cơ” — biến trang từ tĩnh sang động; động ở đây không phải animation `:hover` mà là nội dung đổi theo thời gian (vd. Dân trí), PHP về sau sẽ in bài; demo `document.getElementById` đổi chữ ABC→XYZ và đổi màu, Facebook cuộn xuống thì JS sinh thêm HTML. Thẻ `<script>`: internal / file `.js` external (nhắc inline như CSS); khai báo biến `let` (nhắc `var`, chưa đào sâu), không khai báo kiểu như Pascal; số, chuỗi nháy đơn/kép, mảng — chưa dạy object; tên biến bắt đầu chữ cái (hoặc `$`/`_`), phân biệt hoa thường. Câu điều kiện: `if` / `else` / `else if`, `==` so sánh vs `=` gán, `alert()`; `switch`/`case`/`default` nhớ `break` kẻo rơi xuống case dưới; Inspect → Console xem lỗi. Bài tập: tính thuế theo mốc lương (`if` + `&&` khoảng 7–15 triệu, nối chuỗi số bằng `+`); giao bài âm lịch để buổi sau; demo số ngày/tháng gộp `case`, tháng 2 năm nhuận `%` chia dư + `||`/`&&`; template string ES6 `` `${biến}` `` vs nối `+`. Hết if/switch hôm nay, chưa vòng lặp. Cuối buổi: mẹo Facebook (Inspect xóa comment để chụp, tìm bài theo ID, xem story không bị phát hiện — chặn request Network), khuyên dân CNTT khó chịu thì làm tool, rồi chào — buổi sau chữa bài và trả lời comment.

## Mục lục

```
0:00 Chào live; Q&A học bù / ra trường / phỏng vấn / coder vs developer / autoplay (gộp)
42:19 Bắt đầu bài: HTML xương + CSS mỹ phẩm + JS cơ; tĩnh vs động
47:29 Demo getElementById đổi chữ/màu; Facebook cuộn sinh HTML (gộp)
50:30 Thẻ script; internal vs file .js; biến let; số / chuỗi / mảng; đặt tên (gộp)
56:30 if / else / else if; == vs =; alert (gộp)
1:00:25 switch/case / default / break; Inspect console xem lỗi (gộp)
1:04:22 Bài tập thuế theo lương; && khoảng; nối chuỗi số (gộp)
1:13:10 Bài âm lịch (buổi sau); demo số ngày/tháng + năm nhuận (gộp)
1:21:37 In kết quả; template string `${}` vs nối + (gộp)
1:25:14 Hết if/switch; mẹo Facebook Inspect / tìm bài / story; chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
