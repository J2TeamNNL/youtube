# Livecode Tool chấm đồ án

- **id:** `wrFP4BAp4ok`
- **url:** https://www.youtube.com/watch?v=wrFP4BAp4ok
- **published:** 2021-12-28
- **duration:** 6817s (~1h54m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/livecode-tool-cham-do-an-wrFP4BAp4ok/`](../../transcripts/livecode-tool-cham-do-an-wrFP4BAp4ok/)
- **topics:** do-an, livecode, web
- **series:** Đồ án web cơ bản — livecode tool chấm
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live ~1h54m nối tiếp series Đồ án web cơ bản: phần đầu tâm sự / soft (đi chơi đông–vắng và ngày lễ; ghế công thái học sếp tặng WFH; thói quen đọc thông báo lỗi, tra Google và tiếng Anh kỹ thuật thay vì tắt ngay; xen chia sẻ chuyện tình cảm tuổi trẻ / trách nhiệm — ASR nhiễu nhiều). Sau đó vào livecode xây tool chấm đồ án để khỏi inbox–mở từng repo thủ công: clone/check thư mục nhóm, quét tìm file Word / SQL, báo nhóm thiếu file, rồi tự động tạo DB và import SQL để mở site chấm. Demo Git clone/update; viết script lấy file theo thư mục và đuôi; nối path; in danh sách nhóm có/không file. Phần lớn thời gian với PHP/MySQL (mysqli): `CREATE DATABASE`, connect, import nội dung `.sql`, xử lý lỗi cú pháp (khoảng trắng thừa ở `header Location`, câu `CREATE`), thử export kèm `CREATE`, xóa/tạo lại DB test, vòng `index` import hàng loạt. Gặp file SQL lỗi từ nhóm (ví dụ nhóm 19 file linh tinh) → skip / sửa; tool chạy được nhưng hết giờ chưa kịp chấm thực tế — hẹn nhận nhóm đang xem live và nhắc export SQL chuẩn (dấu chấm phẩy, không rác) trước khi nộp.

## Mục lục

```
0:00 Chào; kế hoạch tâm sự rồi livecode tool chấm; không chủ quan ETA (gộp)
1:58 Tâm sự đi chơi đông–vắng / lễ Tết; ghế công thái học sếp tặng (gộp)
5:43 Đọc lỗi / Google / tiếng Anh kỹ thuật; tự debug khi thầy mất tích (gộp)
8:41 Tâm sự chuyện tình cảm tuổi trẻ / trách nhiệm (ASR nhiễu) (gộp)
25:36 Quan điểm xã hội áp đặt; kết tâm sự (gộp)
27:18 Bắt đầu livecode: tool chấm đồ án / clone & check repo (gộp)
28:19 Mục tiêu: clone → tìm Word/SQL → import DB thay inbox thủ công (gộp)
31:06 Spec tool: quét thư mục, tìm file, báo nhóm thiếu (gộp)
33:03 Demo clone/update repo; Git; bắt đầu code (gộp)
34:24 get file in directories; lấy file theo đuôi (gộp)
39:04 Lọc .sql; kết hợp hai đoạn code (thư mục + file) (gộp)
42:12 Nối chuỗi path; thử chạy; debug kết quả (gộp)
50:02 Báo nhóm có/không file; liệt kê path; chuẩn bị import (gộp)
58:02 Tự động hóa import; CREATE DATABASE / mysqli (gộp)
1:05:02 Load/import SQL; debug kết nối DB (gộp)
1:09:46 Lỗi header Location / khoảng trắng thừa PHP (gộp)
1:13:55 Khuyên học tiếng Anh để tự chủ động học (gộp)
1:19:03 Export kèm CREATE DATABASE; test import lại (gộp)
1:24:14 mysqli connect / error handling; tạo DB test (gộp)
1:27:00 Ý nghĩa tool lâu dài: chạy lại khi nhóm cập nhật (gộp)
1:31:24 Vòng index qua nhóm; import hàng loạt; gặp lỗi SQL (gộp)
1:37:35 File SQL lỗi (nhóm 19); skip / sửa CREATE (gộp)
1:42:06 Demo workflow chấm qua admin; fix index (gộp)
1:48:04 Phân biệt lỗi tool vs lỗi export của nhóm (gộp)
1:51:04 Hết giờ chưa chấm xong; nhắc export SQL chuẩn; tạm kết (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
