# Lập trình web cơ bản buổi 10 JavaScript Array

- **id:** `KjHtFIR79XI`
- **url:** https://www.youtube.com/watch?v=KjHtFIR79XI
- **published:** 2021-11-09
- **duration:** 5226s (~1h27m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-10-javascript-array-KjHtFIR79XI/`](../../transcripts/lap-trinh-web-co-ban-buoi-10-javascript-array-KjHtFIR79XI/)
- **topics:** web, javascript, array
- **series:** Lập trình web cơ bản — buổi 10
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 10 series Lập trình web cơ bản — JavaScript Array: chào, tâm sự làm remote/WFH vs công ty (hiệu suất, tập trung), rồi Q&A nối buổi trước (đặt thẻ `script` cuối HTML sau khi DOM tải; CSS thường để trên; Discord/box riêng) và lan man chuyện nhóm trước khi quay lại bài. Hôm nay bổ sung kiểu dữ liệu mảng để lưu một danh sách nhiều giá trị (ví dụ nhập nhiều số/tên) — object để phần chuyên sâu. Demo form: `input` số + nút nhập / in danh sách; khai báo `[]`, `push` đưa phần tử vào cuối; lưu ý phạm vi `let` trong hàm vs khai báo ngoài để dùng chung giữa các handler. Giải thích index bắt đầu từ 0, `.length`, lấy phần tử bằng `[i]`; mặc định in mảng ra chuỗi cách nhau dấu phẩy. Thực hành `for` duyệt mảng, nối chuỗi (kể cả `+=`), xuống dòng bằng thẻ `<br>`, lọc in số chẵn (`%`); gợi ý clear input sau khi nhập. Gợi ý / giao bài tập: tổng phần tử, min/max, số nguyên tố trong mảng, sinh mảng ngẫu nhiên theo số phần tử nhập vào. Demo `Math.random` + `floor`/`ceil`/`round` để tạo số nguyên trong khoảng; nhắc `push` vs `unshift` / `pop`. Chữa tìm số lớn nhất bằng duyệt vòng lặp (biến tạm, không chỉ gọi hàm sẵn), chữa kiểm tra số nguyên tố bằng đếm ước; cuối buổi chia sẻ demo giao diện/hiệu ứng JS, khuyến khích xem code bạn bè, giao bài, chào.

## Mục lục

```
0:00 Chào live; tâm sự WFH/công ty; Q&A script/CSS/Discord (gộp)
29:17 Bắt đầu bài: JavaScript Array
30:12 Kiểu mảng; lưu danh sách nhiều giá trị (gộp)
31:21 Demo form: input số + nút nhập / in danh sách (gộp)
34:48 Khai báo []; push; phạm vi let trong hàm vs toàn cục (gộp)
38:21 Index từ 0; length; lấy phần tử [i] (gộp)
41:08 for duyệt mảng; nối chuỗi; <br>; lọc số chẵn (gộp)
50:07 Gợi ý bài tập: tổng / min-max / nguyên tố / sinh mảng (gộp)
52:10 Clear input; Math.random & làm tròn; push/unshift/pop (gộp)
1:03:49 Duyệt tìm max; chữa số nguyên tố; chia sẻ demo JS; chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
