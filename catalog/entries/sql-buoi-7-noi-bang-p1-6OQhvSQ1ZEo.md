# SQL Buổi 7 P1 Nối bảng

- **id:** `6OQhvSQ1ZEo`
- **url:** https://www.youtube.com/watch?v=6OQhvSQ1ZEo
- **published:** 2021-10-01
- **duration:** 6038s (~1h41m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/sql-buoi-7-noi-bang-p1-6OQhvSQ1ZEo/`](../../transcripts/sql-buoi-7-noi-bang-p1-6OQhvSQ1ZEo/)
- **topics:** sql, database
- **series:** SQL — buổi 7 P1
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 7 phần 1 (P1) series SQL — nối bảng: chào muộn mic/chat rồi chuyện bên lề dài (mọt sách, tư duy toán, lịch học, tool Facebook/token) trước khi vào lớp. Ôn bài tập thiết kế CSDL cửa hàng (khách hàng–sản phẩm–hóa đơn/chi tiết), quiz khóa chính phức hợp trên hóa đơn chi tiết. Demo dễ hơn với quan hệ 1-N lớp–sinh viên: `CREATE`/`INSERT`, khóa ngoại. Trọng tâm nối bảng: `INNER JOIN` lấy tên lớp; sơ đồ các loại JOIN; Venn với FK (SV luôn thuộc lớp / lớp trống / SV `NULL` mã lớp). Quiz chọn JOIN; `LEFT JOIN` kèm lớp nếu có; `WHERE … IS NULL` tìm SV không lớp; cột trùng tên (ambiguous). Kết hợp thống kê: `COUNT`/`GROUP BY` theo tên lớp qua JOIN; `RIGHT JOIN` để gồm lớp chưa có SV; `COUNT(*)` vs `COUNT(cột)` khi có `NULL`. Ghi chú đây là P1 — P2 sẽ nối nhiều bảng hơn. Cuối buổi chia sẻ Token Facebook (video Mạnh Tuấn), nhắc bài tập / trách nhiệm rồi tạm chào.

## Mục lục

```
0:00 Chào live, mic/chat & chuyện bên lề (gộp)
28:05 Ôn BT hôm trước: thiết kế CSDL cửa hàng
40:20 Quiz PK hóa đơn chi tiết; chữa bài
44:20 Giới thiệu nối bảng; demo SV–lớp
47:50 CREATE/INSERT lớp & sinh viên + FK
50:14 INNER JOIN lấy tên lớp; sơ đồ các loại JOIN
55:24 FK & Venn; lớp trống / SV không lớp
1:00:00 Quiz chọn JOIN; INNER vs LEFT
1:05:15 LEFT JOIN; WHERE IS NULL; cột ambiguous
1:13:20 JOIN + COUNT/GROUP BY; RIGHT JOIN đếm lớp trống
1:22:13 Tạm nghỉ lớp; chia sẻ Token FB; BT & chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
