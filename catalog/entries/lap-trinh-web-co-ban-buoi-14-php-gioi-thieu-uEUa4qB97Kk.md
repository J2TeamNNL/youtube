# Lập trình web cơ bản buổi 14 PHP Giới thiệu

- **id:** `uEUa4qB97Kk`
- **url:** https://www.youtube.com/watch?v=uEUa4qB97Kk
- **published:** 2021-11-25
- **duration:** 6664s (~1h51m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-14-php-gioi-thieu-uEUa4qB97Kk/`](../../transcripts/lap-trinh-web-co-ban-buoi-14-php-gioi-thieu-uEUa4qB97Kk/)
- **topics:** web, php
- **series:** Lập trình web cơ bản — buổi 14
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 14 series Lập trình web cơ bản — PHP Giới thiệu: chào, chat, tâm sự quan điểm thành công (đặt mục tiêu gần/xa, kiếm động lực hàng ngày chứ đừng chỉ dựa ý chí) và giàu (không phải lương cao mà là thu nhập thụ động — không đi làm vẫn tiêu bình thường mà tiền còn tăng), rồi vào bài. PHP là ngôn ngữ phía server: trình duyệt gửi request, server xử lý bằng PHP rồi trả HTML/CSS/JS cho máy người dùng hiển thị — người dùng không cần biết ngôn ngữ gì. Vì sao không viết hết bằng HTML: trang tin tức có hàng nghìn/triệu bài, không ai ngồi code từng file; tạo khung HTML (tiêu đề, nội dung, tin liên quan, bình luận) rồi PHP lấy dữ liệu từ database (sẽ học khóa sau) đổ vào template. Cài môi trường local: Laragon (đang dùng, khuyên) hoặc XAMPP; bật Apache/Nginx + MySQL; code phải nằm trong www (Laragon) hoặc htdocs (XAMPP); gõ localhost/tên-thư-mục, không còn chuột phải mở file như HTML tĩnh. File .php chứa được HTML/CSS/JS lẫn PHP; thẻ PHP và hàm echo in chuỗi (có thể in HTML nhưng thường không in cả trang bằng echo). Biến dùng dấu đô la; không khai báo kiểu (không chặt chẽ); if gần giống JavaScript; echo biến trong chuỗi nháy kép, nháy đơn thì in nguyên tên biến; nối chuỗi bằng dấu chấm chứ không phải cộng (tránh lỗi 3 + "3" như JS). Q&A: token Facebook (lừa đảo giả trang đăng nhập, spam like/follow; xem nhật ký hoạt động, đổi mật khẩu/vô hiệu hóa để hủy token cũ), đăng nhập OAuth bên thứ ba không lấy được mật khẩu — chỉ lấy quyền đã cấp, querySelector. Phần còn lại livecode JavaScript/jQuery: xóa bình luận Facebook có dấu sao — giao diện mobile đơn giản hơn, ghi đè confirm luôn trả về đúng, lấy nội dung comment (không lấy cả tên), click nút Delete, setTimeout khoảng 1 giây/bình luận tránh khóa nick, while tải thêm rồi xóa; máy treo gần cuối, hẹn trả lời thắc mắc buổi sau.

## Mục lục

```
0:00 Chào live; tâm sự thành công và giàu; gợi ý tool JS xóa bình luận (gộp)
15:11 PHP server-side: sinh HTML trả trình duyệt; khung + database (gộp)
24:22 Cài Laragon / XAMPP; www / htdocs; localhost chạy file PHP (gộp)
29:21 File PHP chứa HTML; thẻ PHP; echo in chuỗi (gộp)
35:06 Biến đô la; không khai báo kiểu; if; nháy đơn/kép; nối chuỗi dấu chấm (gộp)
43:15 Q&A token Facebook, lừa đảo, OAuth, querySelector (gộp)
57:51 Tool JS xóa bình luận có dấu sao; giao diện mobile; ghi đè confirm (gộp)
1:07:42 jQuery lấy bình luận; lọc dấu sao; click nút Delete (gộp)
1:29:55 Độ trễ tránh khóa nick; while tải thêm rồi xóa (gộp)
1:49:38 Máy treo; cách giải quyết bài toán; chào buổi sau (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
