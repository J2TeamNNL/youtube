# Lập trình web cơ bản buổi 2 HTML bảng

- **id:** `Y0mgyEp8kFI`
- **url:** https://www.youtube.com/watch?v=Y0mgyEp8kFI
- **published:** 2021-10-09
- **duration:** 4644s (~1h17m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-2-html-bang-Y0mgyEp8kFI/`](../../transcripts/lap-trinh-web-co-ban-buoi-2-html-bang-Y0mgyEp8kFI/)
- **topics:** html, web
- **series:** Lập trình web cơ bản — buổi 2
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 2 series Lập trình web cơ bản — HTML bảng: chào mic/chat, agenda (cách web hoạt động + bảng + mẹo), Q&A mở đầu (HTML không phải ngôn ngữ lập trình, nguồn đọc). Vào học: mở file `.html` đúng trình duyệt (`Open with`), hiện đuôi file, tạo file bằng Sublime; indent/Tab, đường dẫn tuyệt đối vs tương đối (`../`). Giải thích cách đưa trang lên Internet: file local không gửi được đường dẫn máy → cần server chạy 24/7, domain/hosting. Phần chính: dùng thẻ `table` / `tr` / `td` để hiểu bố cục cột (ví dụ Facebook 3 cột); `rowspan` / `colspan`; demo layout 3 cột với `width` %, `bgcolor`, chèn ảnh. Mẹo Sublime multi-cursor (Ctrl+click); chữa bài bố cục bằng bảng + `align`; nhắc bảng chỉ để học — CSS sẽ thay layout. Cuối buổi chia sẻ UX ảnh preview mờ→nét (Google/Facebook) và mẹo lấy ảnh/video đã tải về máy, rồi chào kết.

## Mục lục

```
0:00 Chào live, mic/chat, agenda & Q&A mở đầu (gộp)
13:00 Vào học: Open with trình duyệt; hiện đuôi .html; Sublime
18:00 Đường dẫn tuyệt đối / tương đối (../); phím tắt
26:40 Cách hoạt động web: local → server / domain / hosting
37:40 HTML bảng: table, tr, td; demo dữ liệu
44:00 rowspan & colspan
47:00 Layout 3 cột bằng bảng (width %, bgcolor) + ảnh
53:00 Multi-cursor Sublime; chữa bài bố cục bảng
59:00 Bảng ≠ layout hiện đại; UX ảnh preview mờ→nét (gộp)
1:09:20 Mẹo tải ảnh/video đã xem & chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
