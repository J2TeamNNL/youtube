# Lập trình web cơ bản buổi 9 JavaScript Loop & Input

- **id:** `9HG9MmtpE7Y`
- **url:** https://www.youtube.com/watch?v=9HG9MmtpE7Y
- **published:** 2021-11-07
- **duration:** 4676s (~1h18m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-9-javascript-loop-input-9HG9MmtpE7Y/`](../../transcripts/lap-trinh-web-co-ban-buoi-9-javascript-loop-input-9HG9MmtpE7Y/)
- **topics:** web, javascript, loop
- **series:** Lập trình web cơ bản — buổi 9
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 9 series Lập trình web cơ bản — JavaScript Loop & Input: chào, tâm sự Q&A dài (học liên thông / bằng cấp vs kinh nghiệm; muốn quay lại đi dạy; so cách dạy đại học “nhạt” với cầm tay chỉ việc; vẫn phải dạy đủ cơ bản rồi để tự học + truyền cảm hứng; nước ngoài hướng nghề thực tế hơn) rồi mới vào bài. Hôm nay dạy vòng lặp và tương tác với `input` lần đầu: nhắc lại `if` (điều kiện đúng thì chạy / sai thì dừng), rồi bản chất vòng lặp là lặp lại kiểm tra đúng–sai để chạy tiếp hoặc dừng. `for`: ba biểu thức (khai báo / điều kiện / bước nhảy), thứ tự chạy, demo `console.log` in từ 1 đến n. `while` như bản rút gọn khi đã khai báo biến trước; `do…while` (“đu quay”) chạy thân vòng ít nhất một lần rồi mới kiểm tra — khác `for`/`while` kiểm tra trước. Phần thực hành: lấy giá trị ô nhập (theo thẻ `input` hoặc theo `id` vì id duy nhất), nút bấm in “Xin chào…”, rồi nâng lên kiểm tra số chẵn/lẻ (`%`, gợi ý `type="number"`). Áp dụng lại bài số ngày trong tháng với dữ liệu từ input — lưu ý input ra chuỗi nên so sánh/`==` dễ lệch kiểu; nhắc ép kiểu số trước khi cộng trừ nhân chia (chuỗi `"5"+"3"` vs trừ được ra số), và cảnh báo số thực `0.1+0.2`. Cuối buổi giao bài tập riêng, lan man chia sẻ cách đọc thông tin CPU/chip/RAM khi chọn máy lập trình/web, hẹn ghép buổi trả lời câu hỏi — chào.

## Mục lục

```
0:00 Chào live; Q&A liên thông / bằng cấp / dạy học / tự học (gộp)
15:29 Bắt đầu bài: vòng lặp & tương tác input
16:43 Giới thiệu vòng lặp; bản chất điều kiện đúng/sai
18:43 for: khởi tạo / điều kiện / bước nhảy; demo in 1→n (gộp)
30:48 while; rút gọn từ for (gộp)
33:22 do-while: chạy ≥1 lần vs for/while (gộp)
36:34 Kết hợp bài tập + input HTML; lấy theo thẻ / theo id (gộp)
47:41 Input số: kiểm tra chẵn/lẻ; type number (gộp)
52:08 Số ngày trong tháng từ input; chuỗi vs số khi so sánh (gộp)
1:00:15 Chữa/nhắc bài; ép kiểu; 0.1+0.2 (gộp)
1:08:24 Giao bài tập; chia sẻ chọn máy/CPU/RAM; chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
