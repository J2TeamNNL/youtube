# Lập trình web cơ bản buổi 15 PHP Làm việc với Form

- **id:** `AH9STS4sJSo`
- **url:** https://www.youtube.com/watch?v=AH9STS4sJSo
- **published:** 2021-11-27
- **duration:** 4624s (~1h17m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-15-php-lam-viec-voi-form-AH9STS4sJSo/`](../../transcripts/lap-trinh-web-co-ban-buoi-15-php-lam-viec-voi-form-AH9STS4sJSo/)
- **topics:** web, php, form
- **series:** Lập trình web cơ bản — buổi 15
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 15 series Lập trình web cơ bản — PHP Làm việc với Form: chào, chat, tâm sự ngắn (chí tiến thủ, thực tế khi chọn đối tượng), rồi Q&A lộ trình — đồ án theo cặp bảo vệ cuối khóa, không nên đi làm quá sớm khi mới học cơ bản, khóa này chỉ web/PHP cơ bản (CRUD đơn giản, không MVC/Laravel sâu), khóa sau mới chuyên sâu hơn. Vào bài: tạo form HTML (name dạng snake_case), điền tên / ngày sinh / email / mật khẩu rồi submit; mặc định method GET đẩy dữ liệu lên thanh địa chỉ — không nên dùng GET cho form có mật khẩu. Dùng attribute action trỏ sang file PHP xử lý; file chỉ PHP có thể bỏ thẻ đóng để tránh lỗi khoảng trắng về sau. Lấy dữ liệu bằng `$_GET` / đổi sang `$_POST`; cảnh báo GET lộ mật khẩu trên URL và khi dùng HTTP chung mạng có thể bị bắt gói tin — khuyến khích POST + HTTPS. Mở rộng form: radio giới tính (cùng name, gắn value), select sở thích (nên có value ngắn), textarea mô tả (GET dễ quá tải URL nếu nội dung dài), upload ảnh bắt buộc POST kèm enctype multipart. Cuối buổi giới thiệu sơ đồ khối / thuật toán (ví dụ từng bước lấy nước, vòng lặp “tìm người yêu”) để tư duy trước khi code; hướng dẫn nộp bài PHP qua GitHub/zip; gửi form đăng ký làm việc theo cặp; Q&A về làm nhóm, tự tin tham gia đồ án.

## Mục lục

```
0:00 Chào live; tâm sự chí tiến thủ; chat mở đầu (gộp)
15:00 Q&A đồ án cặp, đi làm, lộ trình khóa PHP / web chuyên sâu (gộp)
27:29 Bắt đầu bài: form PHP — điền thông tin rồi in lại (gộp)
30:17 method GET đẩy lên URL; chuyển POST; action + file xử lý (gộp)
33:05 $_GET lấy theo name; ngày sinh; nhược điểm GET lộ mật khẩu (gộp)
35:50 POST + HTTPS; reload hỏi gửi lại form (gộp)
38:19 Radio giới tính + value; select sở thích; textarea (gộp)
43:14 GET quá dài / upload file cần POST + enctype multipart (gộp)
45:36 Sơ đồ khối thuật toán; ví dụ lấy nước / tìm người yêu (gộp)
1:05:10 Nộp bài PHP (GitHub); form đăng ký làm cặp; Q&A chào kết (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
