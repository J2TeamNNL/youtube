# Lập trình web cơ bản buổi 7 CSS Pseudo & Selector

- **id:** `qJusq22MRvA`
- **url:** https://www.youtube.com/watch?v=qJusq22MRvA
- **published:** 2021-10-26
- **duration:** 5321s (~1h29m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-7-css-pseudo-selector-qJusq22MRvA/`](../../transcripts/lap-trinh-web-co-ban-buoi-7-css-pseudo-selector-qJusq22MRvA/)
- **topics:** css, web, selector
- **series:** Lập trình web cơ bản — buổi 7
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 7 series Lập trình web cơ bản — CSS Pseudo & Selector: chào muộn vì soạn bài dài, lan man Q&A (CNTT khó nhưng vui khi giải được, chọn ngành / không theo vì “hot”, fullstack front+back, crack Windows vs nhóm kích hoạt, Anonymous, tester) rồi mới vào bài. Pseudo-class trên thẻ `<a>`: trình duyệt mặc định (xanh chưa bấm / tím đã bấm, gạch chân, con trỏ); `:hover` đổi màu, nền, cỡ chữ khi trỏ chuột để “kích thích” bấm; `:visited` phân biệt link đã mở; lướt danh sách trên W3Schools, không dạy hết. Selector: ba nút (thường / submit / reset) giống nhau dễ bấm nhầm — attribute selector `button[type=...]` (ngoặc vuông) tô CTA xanh vs reset cảnh báo, thay vì chỉ class/id; nhắc cú pháp này còn dùng khi viết JS. Giao bài tập tổng hợp cuối phần CSS: một trang HTML+CSS giao diện bất kỳ, nộp link GitHub. Demo menu: bảng thể loại phim + `background-image`, Inspect bỏ padding/spacing mặc định; bọc thẻ `a` với `href="#"`, `:hover` chữ đậm/to/đổi nền; `td:hover` vs `td:hover a`. Menu hai cấp `ul`/`li`: ẩn submenu `display:none`, `:hover` hiện `display:block`, `position` relative/absolute đẩy submenu sang phải; `float` + phân biệt `display` block/inline để menu ngang. Cuối buổi: karaoke hàng xóm, nhảy dây, chia sẻ phần mềm (Classic Shell, CCleaner, AnyDesk, VPN, IDM, Logitech Options, Sticky Notes / To Do, ghi chú Zalo) rồi chào — buổi sau sang JavaScript, khô hơn, hết phần giao diện.

## Mục lục

```
0:00 Chào live muộn; soạn bài; Q&A chọn ngành / fullstack / crack Win (gộp)
20:24 Bắt đầu bài: pseudo & selector; thẻ a mặc định trình duyệt
23:23 Pseudo-class :hover (đổi màu / nền / cỡ chữ khi trỏ)
25:04 :visited phân biệt link đã bấm; lướt W3Schools (gộp)
27:32 Selector: 3 nút type khác nhau; attribute [type=...] (gộp)
33:01 Class/id vs selector; nhắc JS sau này; giao bài tập trang web (gộp)
36:07 Demo menu bảng: background-image, inspect padding/spacing (gộp)
42:42 Bọc thẻ a + href="#"; :hover đậm/to/đổi nền; td:hover a (gộp)
48:55 Menu 2 cấp dọc: ul/li, ẩn submenu, :hover hiện, position (gộp)
1:02:27 Menu ngang bằng float; display block vs inline (gộp)
1:06:18 Lan man karaoke / nhảy dây; chia sẻ phần mềm & chào; JS buổi sau (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
