# Lập trình web cơ bản buổi 13 JavaScript DOM BOM Tools

- **id:** `8QxwiQPrNHs`
- **url:** https://www.youtube.com/watch?v=8QxwiQPrNHs
- **published:** 2021-11-23
- **duration:** 3986s (~1h06m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/lap-trinh-web-co-ban-buoi-13-javascript-dom-bom-tools-8QxwiQPrNHs/`](../../transcripts/lap-trinh-web-co-ban-buoi-13-javascript-dom-bom-tools-8QxwiQPrNHs/)
- **topics:** web, javascript, dom, bom
- **series:** Lập trình web cơ bản — buổi 13
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw

## Tóm tắt

Live buổi 13 series Lập trình web cơ bản — JavaScript DOM & BOM & Tools: chào, chat YouTube bản quyền, rồi phân biệt DOM (Document Object Model — tài liệu HTML: document, getElementById, getElementsByTagName/ClassName/Name, innerHTML; lấy nhiều phần tử thì lấy theo index hoặc chạy vòng lặp) và BOM (Browser Object Model — cửa sổ trình duyệt: window, alert, prompt, điều hướng/tab mới; đừng nhầm với Windows hệ điều hành). Q&A khái niệm “Smart” (học hỏi thêm dù đã khá); các hàm get thường dùng; demo hàm gọi nhau, return giá trị, truyền tham số. Liên hệ chuỗi với mảng (index, length; JS có thể ép mảng thành chuỗi khi in). Nhắc tra thêm trên W3Schools; debug bằng console.log / comment từng dòng vì nhiều lỗi không hiện cảnh báo — khoảng 20% thời gian viết tính năng, 80% sửa lỗi; đôi khi viết lại nhanh hơn vá. Mở rộng bài số ngẫu nhiên không trùng: Math.random, đẩy vào mảng, includes, do-while (cẩn thận vòng lặp vô hạn khi khoảng random nhỏ hơn số phần tử cần). Phần Tools: chia sẻ làm tool Facebook — đăng ký app Developer, access token (user vs page admin), quyền hạn chế, Graph API (fields, limit), Access Token Debugger; API công khai bị siết sau scandal; không chia sẻ API ẩn; cảnh báo spam/token/rate limit có thể khóa nick; gợi ý tự động hóa chậm bằng JS trên giao diện thay vì gọi API ồ ạt; nhắc làm bài tập và chào buổi sau.

## Mục lục

```
0:00 Chào live; YouTube bản quyền; giới thiệu DOM & BOM (gộp)
2:20 DOM: document, getElementById, getElementsByTagName, vòng lặp (gộp)
7:27 BOM: window, alert, prompt; khác Windows OS (gộp)
10:15 Q&A Smart; getById/ClassName/Name dùng nhiều (gộp)
13:07 Hàm: return, tham số, gọi hàm trong hàm (gộp)
17:24 Chuỗi vs mảng: index, length; ép kiểu khi in (gộp)
23:38 W3Schools; debug console/comment; 80% sửa lỗi (gộp)
28:35 Số ngẫu nhiên không trùng: includes, do-while (gộp)
35:13 Tool Facebook: app, token, quyền, đăng nhập FB (gộp)
40:51 Graph API: fields, limit; user vs page token (gộp)
52:00 Hạn chế API; API ẩn; an toàn; chào buổi sau (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
