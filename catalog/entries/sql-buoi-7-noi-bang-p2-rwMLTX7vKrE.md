# SQL Buổi 7 P2 Nối bảng

- **id:** `rwMLTX7vKrE`
- **url:** https://www.youtube.com/watch?v=rwMLTX7vKrE
- **published:** 2021-10-03
- **duration:** 6644s (~1h51m)
- **kind:** live
- **subtitle_source:** auto (ASR — có thể nhiễu / sai từ)
- **folder:** [`transcripts/sql-buoi-7-noi-bang-p2-rwMLTX7vKrE/`](../../transcripts/sql-buoi-7-noi-bang-p2-rwMLTX7vKrE/)
- **topics:** sql, database
- **series:** SQL — buổi 7 P2
- **channel:** UCnL9cDH4iZcLmOIF8SzNGYw
- **liên quan:** [P1 Nối bảng](./sql-buoi-7-noi-bang-p1-6OQhvSQ1ZEo.md) (`6OQhvSQ1ZEo`)

## Tóm tắt

Live buổi 7 phần 2 (P2) series SQL — nối bảng (tiếp [P1](./sql-buoi-7-noi-bang-p1-6OQhvSQ1ZEo.md)): chào mic/chat rồi chuyện lộ trình khóa, nghề DB/SQL, đồ án và Q&A dài trước khi vào lớp. Ôn/INSERT lại lớp–sinh viên (kèm lớp trống); mở rộng N-N qua bảng **điểm** (sinh viên–môn), khóa chính phức hợp + khóa ngoại. Ôn JOIN hai bảng: SV kèm lớp nếu có; `COUNT`/`GROUP BY` theo lớp với `RIGHT`/`LEFT JOIN` để gồm lớp chưa có SV; `COUNT(*)` vs `COUNT(cột)`. Trọng tâm P2: nối **ba bảng** SV–điểm–môn (`INNER` rồi chuỗi `LEFT` để mọi SV kèm điểm/môn nếu có). Thống kê nâng cao: `AVG` điểm trung bình theo lớp / theo môn kết hợp JOIN + `GROUP BY`; tư duy rút gọn JOIN khi không cần đủ mọi bảng. Cuối buổi chia sẻ phím tắt Windows/tool, nhắc đây là buổi cuối SQL cơ bản, bài tập và lịch khóa sau rồi tạm chào.

## Mục lục

```
0:00 Chào live, mic/chat & chuyện lộ trình/nghề (gộp)
33:18 Chữa BT / Q&A; phím tắt tool; vào lớp
37:36 Ôn SV–lớp; INSERT lớp trống; gợi ý N-N
42:39 Tạo môn & bảng điểm (N-N qua bảng trung gian)
47:56 PK phức hợp điểm; FK; INSERT điểm
51:26 Ôn JOIN: SV kèm lớp; COUNT theo lớp; lớp trống
1:00:56 Nối 3 bảng: SV–điểm–môn (INNER)
1:08:32 LEFT JOIN chuỗi: mọi SV kèm điểm/môn nếu có
1:18:38 AVG điểm TB theo lớp (JOIN + GROUP BY)
1:27:35 AVG theo môn; tư duy rút gọn JOIN
1:40:00 Phím tắt Windows; BT buổi cuối & chào (gộp)
```

> Lưu ý: phụ đề auto ASR có thể nhiễu; ưu tiên nghe lại đoạn quan trọng nếu cần trích dẫn chính xác.
